import { DefaultScene, Colors } from "engine";

export default class extends DefaultScene {
	async setup() {
		const c = this.Rectangle({
			// x: 0,
			// y: 0,
			// width: 100,
			// height: 100,
			// background: Colors.WHITE,
			// strokeWidth: 1,
			// stroke: Colors.BLACK,
			// opacity: 1,
			// rotate: 0,
			// dashOffset: 0,
		});
	}
}
